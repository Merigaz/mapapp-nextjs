import type { ThemeConfig } from "antd";

const theme: ThemeConfig = {
  token: {
    fontSize: 16,
    colorPrimary: "#A48C59",
    colorPrimaryText: "#fcc501",
    colorText: "#343D4B",
  },
};

export default theme;
